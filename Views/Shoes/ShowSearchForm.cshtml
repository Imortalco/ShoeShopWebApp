
<h4>Търсете по модел:</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ShowSearchResults">
            <div class="form-group">
                <label for="SearchModel" class="control-label">Въведете модел:</label>
                <input name="SearchModel" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Търси" class="btn btn-primary btn-lg active" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Обратно към списъка</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
